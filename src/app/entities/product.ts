export class Product {
  price: number;
  color: string;
  title: string;

  constructor(price: number, color: string, title: string) {
    this.price = price;
    this.color = color;
    this.title = title;
  }

}
